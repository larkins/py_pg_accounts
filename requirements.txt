flask>=2.3.0
flask-sqlalchemy>=3.0.0
flask-login>=0.6.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
pyyaml>=6.0
werkzeug>=2.3.0
pillow>=9.5.0
requests>=2.31.0
bcrypt>=4.0.0
reportlab>=4.0.0

# OCR pipeline (2026-08-04 refactor — replaces gemma4 vision LLM)
# pytesseract is the Python wrapper for the system tesseract binary.
#   apt install tesseract-ocr          # required
#   apt install tesseract-ocr-eng      # default language pack
#   apt install poppler-utils          # required for PDF receipts (pdftoppm)
pytesseract>=0.3.10
pdf2image>=1.17.0
